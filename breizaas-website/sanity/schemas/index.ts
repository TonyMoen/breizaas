import { albumSchema } from './album';
import { trackSchema } from './track';
import { videoSchema } from './video';
import pressKit from './pressKit';

export const schemaTypes = [albumSchema, trackSchema, videoSchema, pressKit];
